/**
 * Default map constants.
 * Base coordinates are now stored in `company_settings` table.
 * These are only fallback values used when the DB record is empty.
 */

/** Fallback center of Lithuania – used when no company_settings base is set */
export const LITHUANIA_CENTER: [number, number] = [55.3, 23.9];
export const LITHUANIA_ZOOM = 7;
