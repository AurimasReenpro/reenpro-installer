// Canonical option sets for operational/BI fields on a site.
// Shared by CreateSiteModal, admin SiteDetails, and the Reports BI dashboard so the
// inputs and the report filters always speak the exact same values.

export const ROOF_TYPES = ['Skarda', 'Čerpės', 'Prilydoma', 'Žemė'] as const;
export const ROOF_ANGLES = ['Plokščias', 'Standartinis', 'Status'] as const;
